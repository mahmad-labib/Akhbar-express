const config = {
    // Start port
    port: 3030,

    // Database configuration
    database: {
        DATABASE: 'test',
        USERNAME: 'adam',
        PASSWORD: '01030',
        HOST: 'localhost'
    }
}

module.exports = config;